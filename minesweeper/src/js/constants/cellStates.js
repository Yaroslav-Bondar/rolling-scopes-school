const OPENED_CELL_STATE = 'opened';

const MARKED_CELL_STATE = 'marked';

const MINED_CELL_STATE = 'mined';

const NUMBER_CELL_STATE = 'number';

module.exports = {
  OPENED_CELL_STATE,
  MARKED_CELL_STATE,
  MINED_CELL_STATE,
  NUMBER_CELL_STATE,
};
